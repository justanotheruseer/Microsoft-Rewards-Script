steps:
  test:
    image: alpine
    commands:
      - echo "Step is running!"
      - ls -la
      
  build:
    image: docker:dind
    privileged: true
    commands:
      - docker info
      - docker build -t 192.168.1.20:5000/refarmer-v2:latest .
      - docker push 192.168.1.20:5000/refarmer-v2:latest
